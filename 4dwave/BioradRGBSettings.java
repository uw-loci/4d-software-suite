class BioradRGBSettings{	boolean					ch_1_on;// process channel 1 of Biorad 1024 file?	boolean					ch_2_on;// process channel 2 of Biorad 1024 file?	boolean					ch_3_on;// process channel 3 of Biorad 1024 file?	int						num_channels;	boolean					combine_channels;// if true combine the channels into a single 24-bit image		boolean					got_settings;		public BioradRGBSettings()	{		ch_1_on = false;		ch_2_on = false;		ch_3_on = false;		combine_channels = true;		num_channels = 1;				got_settings = false;				return;	}// init}// end of BioradRGBSettings